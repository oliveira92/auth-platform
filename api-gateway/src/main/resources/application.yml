spring:
  application:
    name: api-gateway
  config:
    import: "optional:aws-parameterstore:/config/auth-platform/api-gateway/"
  profiles:
    active: ${SPRING_PROFILES_ACTIVE:local}
  cloud:
    gateway:
      default-filters:
        - DedupeResponseHeader=Access-Control-Allow-Credentials Access-Control-Allow-Origin
      globalcors:
        cors-configurations:
          '[/**]':
            allowedOriginPatterns: "*"
            allowedMethods: "*"
            allowedHeaders: "*"
            allowCredentials: true
      routes:
        # Auth Service routes (public - login, refresh, validate)
        - id: auth-service
          uri: http://${AUTH_SERVICE_HOST:auth-service}:${AUTH_SERVICE_PORT:8081}
          predicates:
            - Path=/api/v1/auth/**

        # Authorization Service routes (protected)
        - id: authorization-service
          uri: http://${AUTHZ_SERVICE_HOST:authorization-service}:${AUTHZ_SERVICE_PORT:8082}
          predicates:
            - Path=/api/v1/authorization/**, /api/v1/applications/**, /api/v1/roles/**

        # Swagger UI aggregation
        - id: auth-service-swagger
          uri: http://${AUTH_SERVICE_HOST:auth-service}:${AUTH_SERVICE_PORT:8081}
          predicates:
            - Path=/auth-service/v3/api-docs
          filters:
            - RewritePath=/auth-service/(?<segment>.*), /${segment}

        - id: authorization-service-swagger
          uri: http://${AUTHZ_SERVICE_HOST:authorization-service}:${AUTHZ_SERVICE_PORT:8082}
          predicates:
            - Path=/authorization-service/v3/api-docs
          filters:
            - RewritePath=/authorization-service/(?<segment>.*), /${segment}

server:
  port: ${SERVER_PORT:8080}
  shutdown: graceful

auth:
  gateway:
    jwt-public-key: ${JWT_PUBLIC_KEY:}
    jwt-issuer: ${JWT_ISSUER:auth-platform}

aws:
  region: ${AWS_REGION:us-east-1}

management:
  endpoints:
    web:
      exposure:
        include: health,info,metrics,prometheus,gateway
  endpoint:
    health:
      show-details: always
    gateway:
      enabled: true

springdoc:
  swagger-ui:
    path: /swagger-ui.html
    urls:
      - name: auth-service
        url: /auth-service/v3/api-docs
      - name: authorization-service
        url: /authorization-service/v3/api-docs

logging:
  pattern:
    console: "%d{yyyy-MM-dd HH:mm:ss.SSS} [%thread] [%X{correlationId}] %-5level %logger{36} - %msg%n"
  level:
    com.authplatform: DEBUG
    org.springframework.cloud.gateway: INFO
