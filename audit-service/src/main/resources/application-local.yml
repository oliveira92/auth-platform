spring:
  datasource:
    url: ${DB_URL:jdbc:mysql://localhost:3306/authplatform?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC}
    username: ${DB_USERNAME:authplatform}
    password: ${DB_PASSWORD:authplatform}
