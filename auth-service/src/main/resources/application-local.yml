# Local profile - uses OpenLDAP test container and local RSA keys
# Run: scripts/keys/generate-rsa-keys.sh to create RSA keys

spring:
  cloud:
    aws:
      region:
        static: ${AWS_REGION:us-east-1}
      endpoint: ${AWS_ENDPOINT_OVERRIDE:http://localhost:4566}
      credentials:
        access-key: test
        secret-key: test

aws:
  endpoint-override: ${AWS_ENDPOINT_OVERRIDE:http://localhost:4566}
  region: us-east-1

auth:
  ldap:
    url: ${LDAP_URL:ldap://localhost:389}
    base-dn: "dc=authplatform,dc=com"
    user-dn: "cn=admin,dc=authplatform,dc=com"
    password: "admin"
    user-search-base: "ou=users"
    user-search-filter: "(uid={0})"
    group-search-base: "ou=groups"
    group-search-filter: "(memberUid={0})"

  # RSA keys are loaded from LocalStack Secrets Manager by AwsSecretsLoader on startup.
  # These blank defaults are overwritten at runtime — do NOT set JWT_PRIVATE_KEY here.
  jwt:
    private-key: ${JWT_PRIVATE_KEY:}
    public-key: ${JWT_PUBLIC_KEY:}

logging:
  level:
    com.authplatform: DEBUG
    org.springframework.ldap: DEBUG
